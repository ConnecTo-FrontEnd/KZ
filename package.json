{
  "dependencies": {
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5",
    "sass": "^1.54.0"
  },
  "scripts": {
    "start": "run-p watch server",
    "sass": "sass src/sass:src/style",
    "watch": "npm run sass -- --watch",
    "server": "live-server"
  }
}
